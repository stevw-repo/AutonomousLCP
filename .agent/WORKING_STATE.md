# AskLegal Legal Database Pipeline — Working State

## 2026-09-10 — Task 10 final integration (`/root`, local desktop)

## Current goal

Finish the two-family Hong Kong V1 local prototype through truthful live-source,
provider, Review/Approval, promotion, rollback/recovery, and final admission
evidence. Commit and push the stable implementation to the renamed GitHub
repository. Any unavailable recovery or external input must remain visibly
`NOT_READY`/`NOT_ADMITTED`.

## Authorization and interaction boundary

- The user granted standing authority for all in-scope Task 10 source,
  provider, Pinecone, host, credential, SQL, recovery, reboot, transition,
  cleanup, and prototype deployment operations. Exact fingerprinted plans,
  receipts, limits, readbacks, and rollback boundaries still apply.
- The user explicitly requested commit and push to
  `https://github.com/stevw-repo/AutonomousLCP.git`. Commit
  `456268baea08dacedd8a3db708d4a0ab5c21ca6b` (`Complete Hong Kong V1 local
  execution path`) was pushed without force to `origin/main` on 2026-09-10.
- Standing authority cannot supply the local sudo password, create a genuine
  named-human Approval decision, manufacture publisher facts, or accept unseen
  third-party terms. Never place credentials in chat, Git, plans, reports, or
  continuity files.

## Verified implementation state

- The local implementation covers resumable Cases/Legislation acquisition,
  Control-to-Legal dispatch, strict Legal processing, immutable package
  publication, Review/Approval, promotion, Gate A–G evidence parsing, provider
  evaluation, rollback/restoration, host reconciliation/apply, and credential
  rotation. Independent source, provider, promotion, and credential reviews
  closed their confirmed Critical/Important findings.
- Final GLD discovery/observation code binds exact authority lineage and retained
  discovery basis, streams bounded bytes, permits no redirect in the one-request
  listing budget, and enforces one monotonic operation deadline. It cannot click
  or accept terms and cannot claim a complete source window.
- Offline Patchright closure is present: Chromium/headless-shell `1234`, FFmpeg
  `1011`, 598 files / 685,353,456 bytes, fingerprint
  `sha256:fa4d31234885e3dd42df99d947c27d5c0a3d62e24d5818fa17aa16ac7f99aabf`.
  The Debian closure is 114 archives / 99,582,500 bytes, manifest fingerprint
  `sha256:62f351d79c13eac12a99665580a5fb223ae3baa013507c3b18fa2ee0138ce671`.
- The seven-credential/five-service rotation is restart-safe and value-free in
  retained evidence. It binds current source/build/image identity, the exact 7
  predecessor and 7 candidate hashes, a fixed authorized plan fingerprint, a
  dual-vault-network helper, IAM and application authentication readback,
  rollback, and resumable terminal cleanup. Its final combined focused gate was
  99 passed; Ruff, Pyright, Bash, boundary, and diff checks were green.
- Private candidate staging remains under ignored `var/`: exactly 25 mode-0600
  regular files in a mode-0700 directory. The value-free staging receipt is
  `var/hk-v1/credentials/vault-primary-application-staging.json`, fingerprint
  `sha256:62fc907a3f8ca721a7f89fc9b9067660888999b5f328049bee28659dd1ab2b4e`.
  The accidentally displayed predecessor vault credential must remain treated
  as compromised and must never be repeated or deployed.
- Host apply uses only source-bound `hk-v1-candidate` image tags, verifies exact
  build-result IDs, atomically installs a root-owned `0400` deployment-image
  manifest, executes manifest digests rather than mutable tags, verifies
  bootstrap/systemd/timers/containers/health/orphan absence/firewall facts, and
  proves semantic predecessor rollback on failure and terminal replay.
- Recovery preflight
  `var/hk-v1/recovery/preflight-not-ready-2026-09-09.json` remains honestly
  `NOT_READY`, fingerprint
  `sha256:ccf1ab00169f55f3ec385bde89d39f3573224bf273d38ded15b868ed2d4427c3`,
  because SQL and Scheduler live inputs are absent. This does not deadlock local
  prototype startup, but Gate F and final admission remain blocked.

## Latest build and proof

- Final source-bound phase-one plan fingerprint:
  `sha256:b470c958ee30885941d7a4d7349d18bfa2c43de1981c6c3bf2c7e0fe0280432b`.
- Authority-gated offline phase one completed with the intended result
  `PHASE_COMPLETE_REPLAN_REQUIRED`; it built all five candidate images and
  performed no service replacement, credential change, registry push, or
  deployment.
- Final application-build-results fingerprint:
  `sha256:3c51c29f66fd111bedaa02a1c5446023d097fd7a6bd87ffdbc912a9f45f93630`.
  Post-build facts fingerprint:
  `sha256:0f70adddf58652b1accae4e0285c01d5702d2cff5770b3a2fa24cb4b9e05942c`.
- Read-only OCI proof passed and is retained at
  `var/hk-v1/host/application-image-oci-proof-final-2026-09-10.json`,
  fingerprint
  `sha256:f0975edd50acb067f93dd3b85fdde2eedc2610c4ce68db4929e9a555d247e6e4`.

## Latest validation

- Apps/packages inventory: effective `4,717 passed, 3 skipped`; the 12 local
  socket tests separately passed outside the sandbox. One stale HK Cases
  fingerprint assertion was corrected and its affected gate passed 81.
- Tools shard: `1,362 passed, 2 skipped`. Canonical package generators were run
  twice with byte-identical outputs; focused rulebook/conformance passed 252;
  contract validation passed at manifest fingerprint
  `sha256:64c4544b2d9b6039f7b88de61d7605077d460637d5660a2776655400beb89d66`.
- The three long source-continuation regressions passed in 1,550.62 seconds
  after refreshing only their synthetic Matrix/Cases authority fixtures.
  Production authorization remains strict. The earlier complete source shard
  was `558 passed, 3 failed`; those exact three failures are the now-green
  regressions.
- Candidate image/build/wheelhouse tests passed 57 after the final Legal
  installed-tree measurement correction.
- Final post-edit gates passed: Ruff lint and format across 706 files, strict
  Pyright across 553 files with zero errors/warnings, Python boundary across
  419 files / 551 reviewed exceptions, contract validation, unit/provisioning
  render checks, Bash syntax, documentation tests (`9 passed`), and diff check.
- Read-only pre-commit audit inspected 513 candidates (168 modified tracked,
  345 untracked): no secrets/private keys/token URLs/JWTs, unsafe symlinks,
  world-writable files, junk, ignored `var` artifacts, or unexpected binaries.
  The 3.61 MB tokenizer asset and public Sectigo certificate are intentional.

## Current blocker and exact next steps

1. Run the root-only credential-rotation preflight. This requires the operator
   to enter the local sudo password in their own terminal; it is an OS input,
   not another authorization decision. Inspect the value-free plan and then run
   its exact plan-fingerprint-bound transaction.
2. Reconcile a phase-two host plan from final image results, successful
   credential report, current root facts, and the retained recovery limitation;
   apply and read back local prototype startup.
3. Run authorized no-click GLD discovery. Human-inspect the exact retained
   observation before freezing a challenge contract and distinct OBSERVE
   authority. If terms/consent appears, stop before accepting it.
4. Produce T1/T2 and the real Legal proposal; perform the two proposal-bound
   provider/Pinecone evaluation runs; obtain genuine named-human Review
   Approval; promote/read back/rollback/restore.
5. Produce the strongest truthful recovery/reboot/Gate A–G evidence. If live
   SQL/Scheduler recovery inputs remain unavailable, finish visibly
   `NOT_ADMITTED` rather than inventing proof.

## External effects in this continuation

- Performed authorized offline local Docker builds and read-only Docker
  inspection. Five `hk-v1-candidate` images and ignored build evidence were
  produced. No image was pushed or run as an application service.
- Commit `456268baea08dacedd8a3db708d4a0ab5c21ca6b` was pushed to the renamed
  GitHub `origin/main`. No source/browser request, terms acceptance,
  Azure/model/embedding/Pinecone call, vault IAM mutation, credential install,
  SQL/Scheduler mutation, service restart, firewall/routing change, reboot,
  deletion, or release publication has yet occurred in this continuation.

The repository remains intentionally dirty with the complete Task 10 work.
Preserve all tracked and untracked implementation artifacts; ignored `var/`
contains private/runtime evidence and must not enter Git.
