# AskLegal Legal Database Pipeline — Working State

Updated: 2026-08-25 — docpro-MS-7D99 (Ubuntu 24.04.4 LTS, x86-64), branch `main`

## Current goal

Advance dependency-ordered HKV1-0 through HKV1-10 to one working, honestly
scoped full-Hong-Kong local V1. Continue safe reversible local implementation
and validation; stop only for a genuine user decision, new external authority,
or destructive/live-host operation.

## Current checkpoint

The first accepted Hong Kong Cases later-treatment checkpoint is implemented.
Source-neutral package `0.11.0`, fingerprint
`sha256:545b9bb4ebb9dbfba16dc7bd3d54e1b83e698991331d4ffe30fe2c3ab82f77d9`,
now executes all 13 frozen ADR 0059 whole-judgment discovery cases and the
complete P001 segmented-reading/silent-incompleteness pair. Strict separated
model/reference/observation artifacts cover empty and bare-citation judgments,
implicit and unresolved identities, material versus bare leads, separately
attributed majority/dissent candidates, zero-proposition judgments, complete
cross-segment context, missing required segments, corrected-reasons inventory,
and Traditional Chinese/mixed-language reasoning.

The evaluator derives its result from facts without receiving the case ID.
Silence never proves no treatment, incomplete ledgers cannot become supported
zeroes, correction comparison decides no legal effect, and the result grants
zero provider calls, treatment relationships, Search Records, release, or
external effects. This completes 13/155 frozen treatment cases and 13/53
semantic treatment cases; candidate analysis and all deterministic treatment
consequences remain.

## Verified current validation

- Complete locked shipping command:
  `python3 -m tools.dev_test --uv /home/docpro/.local/bin/uv --node /home/docpro/.local/bin/node`.
- Result: strict Pyright 0; Ruff clean and 454 files formatted; Python boundary
  255 files with the unchanged 12 reviewed exceptions; architecture 5 apps,
  14 packages, 80 dependency edges, and 31 capability ports; contracts and
  generated package reproduction pass; 1524 tests passed and 4 intentional
  opt-in tests skipped in 181.91 seconds. The command used local-loopback
  permission required by four socket tests; those four also pass directly.
- Direct external-attestation suite: 10 passed in 10.61 seconds. Complete HKEX
  Regulatory/package/attestation sweep: 353 passed in 86.94 seconds.
- Attestation processing-build fingerprint:
  `sha256:c9f9a1ee834ca85baca2a88380e5693f97b1579360da501fc39aeba1fae6d219`;
  architecture-report fingerprint:
  `sha256:a1e7eb300235c79c0dc40c86a7834bc43f7b8364d5052dc0fa9759a428e3cae2`.
- Direct treatment-discovery plus Cases package suite: 85 passed in 73.53
  seconds. Treatment-discovery plus regenerated HKEX attestation: 23 passed in
  10.50 seconds. Targeted Ruff is clean and targeted strict Pyright reports 0
  errors, 0 warnings, and 0 information.
- Direct semantic-risk suite: 14 passed. Combined Cases connector, listing,
  ledger, output, admission, semantic-task, semantic-materiality,
  semantic-content, semantic-boundary, semantic-risk, and package sweep: 230
  passed in 71.38 seconds.
- Two consecutive generated 559-file Cases package trees had identical canonical
  tree digest `86ff3164e5e60679205d515f4b5d0a6857bfbfa44b7bb204a0cd17898063b559`.
- Complete HKEX Regulatory/package sweep: 343 passed in 74.00 seconds.
- Final identity checkpoint direct suite: 15 passed.
- Two consecutive generated 647-file HKEX package trees had identical canonical
  tar digest `174ef8cb9d1bf81a352143f6d22b4e29ba6e89ec59c168211c6702fa1ef7f1a9`.
  Package construction and the repository contract validator also independently
  report deterministic reproduction.
- Current contract package fingerprint:
  `sha256:92b5193ca9fa45d2260ee2012cd350818136f4d12b1d93e763a67b827d15c455`.

## V1 position

- HKV1-0: GLD remains the earliest originating Gazette source and HKeL the
  recovery/backcapture source. Lawful repeatable GLD acquisition, complete HKeL
  recovery coverage, common cutoffs, and explicit exclusions remain.
- HKV1-1: local engineering gates are complete; one successful hosted Azure
  Pipelines execution still needs observation.
- HKV1-2: acquisition accounting, traversal, redirects, bounded locators,
  schedulable inventory, retries, outage profiles, and cycle coverage exist.
  Browser-session evidence procedures for GLD and required HKeL evidence paths
  remain.
- HKV1-3: Legislation package `0.41.0` has the source-neutral bilingual,
  lifecycle, reconstruction, rendering, partition, fallback, and reconciliation
  machinery. All three scopes remain `NOT_READY` because no authentic HKeL
  XML/XSD/PDF/specification evidence has been admitted and no real baseline has
  been built.
- HKV1-4: Cases package `0.11.0` has a seven-role source register, six-family
  scope foundation, deterministic listing accounting, and all 54 frozen
  deterministic proposition cases. Its strict two-family/eight-kind semantic
  preflight authorizes no provider call; its separately permissioned evaluator
  fixtures execute all 78 frozen synthetic proposition cases. The first 13/155
  later-treatment cases now execute the complete whole-judgment discovery
  checkpoint without provider or legal-effect authority. It has zero admitted
  endpoints, concrete court-year scopes, real evidence, executable semantic
  provider workflow, candidate-analysis suite, treatment graph, or baseline.
- HKV1-5: all 284 source-neutral HKEX cases execute and one exact external
  build-compatibility attestation passes. Exact five-role endpoint contracts,
  authentic component inventories/trees, admitted tokenizer/model profiles,
  protected real-source evaluation, named owner/system admissions, real
  register allocation, releases, and both board admissions remain.
- HKV1-6: Principles remains blocked on publisher/title/licence selection. If
  omitted, the output cannot be called the complete Hong Kong jurisdiction
  defined by the accepted design.
- HKV1-7: exact model, embedding, tokenizer, prompt, evaluation, cost/quota, and
  Pinecone target profiles remain unadmitted.
- HKV1-8: durable proposal read-back, Review decision, Approval consumption,
  revocation/invalidation, execution authorization, first Effect Intent, claim
  read-back, and disabled fake handler are implemented. Real current-authority/
  capability evidence, provider composition, backup, cutover, reverse-swap, and
  rollback remain; no provider handler is registered.
- HKV1-9: current image deployment, credential rotation, timers, telemetry,
  network-risk disposition, reboot/recovery, and live acceptance remain.
- HKV1-10: no complete real baseline, Approval, activation, update cycle, or
  rollback proof exists.

## Working copy and authorization

- The last pre-checkpoint base was pushed `d165161`; a read-only fetch check on
  2026-08-25 found no divergence at that point. Inspect Git directly for the
  current checkpoint hash and remote alignment.
- The post-`d165161` V1 changes listed below form one complete validated
  checkpoint. The user explicitly authorized its commit/push and asked work to
  stop immediately afterward.
- Continued safe local V1 implementation and completed isolated local package,
  SQL, Durable Task, and image proofs are authorized.
- No authority exists for any external legal-source fetch, provider/model/
  Pinecone call, deployment, credential rotation, stopping live host services,
  applying migrations to live SQL, or production mutation. No commit/push
  authority extends beyond this checkpoint.
- The disposable demo branches are informational only. Do not import or
  cherry-pick their code.

## Current changed areas

- HKEX source-neutral inventory/state/continuity/rendering/partition/coverage/
  identity contracts, all 284 conformance cases, generator, package, loader,
  and tests under `packages/legal-desks/` and `tools/build_hk_regulatory_rulebook.py`;
  the external attestation contract, isolated runner, builder, strict schemas,
  rule, result, and immutable proof bundle.
- Hong Kong Legislation reconstruction, canonical rendering/partitioning,
  package `0.41.0`, generator, and tests.
- Hong Kong Cases listing, Coverage Ledger, output-validation, correction,
  package-security, workflow-identity, ADR 0065/0066 semantic-task preflight,
  semantic-materiality, semantic-content, semantic-boundary, and semantic-risk
  evaluator kernels and the 13-case whole-judgment treatment-discovery
  evaluator; package `0.11.0`; disabled source-
  access register; generator; connector foundation; schemas; separated model/
  reference/observation fixtures; expected results; and tests.
- Corpus traceability lookup, proposal-member trust chain, Review/Promotion
  readers, Approval/execution lifecycle, migrations `000004` through `000009`,
  disabled first-intent handler, and tests.
- V1 continuity files, contracts, local conformance inputs, and generated
  manifests. See `git status --short` for the exact dirty inventory.

## Exact next steps

1. Keep production V1 composition unavailable unless the manifest's exact
   capability-profile reference resolves to current admitted HKV1-7 evidence;
   no side override or global enable flag may substitute.
2. Acquire and preserve authentic HKeL format evidence when separately
   authorized; it is the dependency for real Legislation parsing/admission.
3. Extend the Cases register with exact official Judiciary endpoint,
   enumeration, rate, format, and rights contracts only from preserved source
   evidence; freeze concrete court-year scopes only from proved boundaries.
4. After the requested stop, resume with the 40-case source-neutral treatment-
   candidate semantic-analysis checkpoint; then implement deterministic
   validation, Legal Desk decision, graph/note/record/release consequences, and
   evaluation boundaries without enabling a provider call.
5. Keep the proved Promotion `BEGIN` service and all provider handlers outside
   real composition until exact HKV1-7 profiles and current capability evidence
   are admitted.

## Open decisions requiring the user later

- Select exact Principles publishers/titles/editions and continuing rights, or
  accept the narrower complete-primary-law/binding-cases/HKEX V1 label.
- Authorize bounded official-source acquisition after applicable terms are read
  and preserved; GLD Turnstile/CAPTCHA must not be bypassed.
- Separately authorize later live-host mutation, credential rotation, external
  model/embedding/Pinecone calls, deployment, and any commit/push after this
  checkpoint.
