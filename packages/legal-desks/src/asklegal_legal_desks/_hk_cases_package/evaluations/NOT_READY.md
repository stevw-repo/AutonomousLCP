# Hong Kong Cases evaluations not ready

No protected real-judgment, proposition, treatment, multilingual, or retrieval evaluation is admitted.

All 54 frozen deterministic structure, Coverage Ledger, candidate, evidence,
renderer, output, correction, package, security, and workflow-identity cases are
executable using source-neutral synthetic facts. They prove no authentic
judgment parser, semantic proposition workflow, sealed real-judgment result, or
real coverage. Workflow `ELIGIBLE` remains only an effect-free conformance
conclusion; this package creates no admission and authorizes no provider call.

The two ADR 0066 task families and all eight closed request kinds now have
strict executable preflight envelopes and supplementary package fixtures. A
valid preflight proves only that the complete bounded request may be considered
by later admitted provider composition. It authorizes zero provider calls,
accepts zero semantic results, and executes none of the 78 frozen semantic
correctness cases.

The first frozen semantic checkpoint now executes all 18 `SEM-MAT` cases using
small synthetic model-facing inputs, separately inventoried evaluator-only
Reference Proposition Maps, adjudicated observation fixtures, and a
deterministic comparator. Six within-checkpoint materiality high-risk pairs
reject cross-member observations; cross-checkpoint pair P019 is now complete
through `SEM-RSK-016`. This proves the materiality evaluator and frozen
synthetic assertions, not the two-pass model workflow. Every sealed real
judgment, provider run, threshold/profile, repetition set, and admission remains
absent.

The second frozen semantic checkpoint executes all 18 `SEM-CNT` cases through
the same separated model/reference/observation trust boundary. Its evaluator
requires every material qualification, exception, definition, burden, minimum
context, application, result, exact quotation, ordered non-contiguous range,
structure, dependency, ambiguity, locator consequence, and faithful equivalent
meaning asserted by the hidden map. It deliberately does not require preferred
prose equality. Pairs P006-P008 reject their cross-member observations. Together
the two checkpoints execute 36/78 synthetic semantic cases; 42 remain. These are
canonical adjudicated observation fixtures, not provider results or workflow
admission evidence.

The third frozen semantic checkpoint executes all 22 `SEM-BND` cases. Its
evaluator requires exact cumulative-versus-independent proposition boundaries,
issue scope, opinion inventory and path, joint/lead/concurrence/dissent/obiter/
plurality authority role, express adoption, evidence range, primary-unit use,
selection, and quarantine state. Pairs P009-P015 reject their cross-member
observations, including unsupported plurality reasoning that must not be
manufactured into a majority holding. Together the three checkpoints execute
58/78 synthetic semantic cases; the 20 `SEM-RSK` cases remain. These fixtures
still prove no provider workflow, authentic judgment result, admission, record,
release, or external effect.

The fourth frozen semantic checkpoint executes all 20 `SEM-RSK` cases. It
requires exact original-language and court authority, auxiliary-only translation
use, mixed-language and historical-format preservation, complete segment and
dependency accounting, supported parser structure, honest serving-limit split
or quarantine, uncertainty quarantine, source-text-as-evidence handling for
hostile quoted instructions, and immutable corrected-reasons re-evaluation.
Pairs P016-P018 reject their cross-member observations and `SEM-RSK-016`
completes cross-checkpoint pair P019 against the proven zero in `SEM-MAT-017`.
All 78 frozen synthetic semantic cases now execute, but they remain canonical
adjudicated observations rather than provider or sealed real-judgment results.
No workflow admission, Search Record, release, activation, or external effect
is authorized.
