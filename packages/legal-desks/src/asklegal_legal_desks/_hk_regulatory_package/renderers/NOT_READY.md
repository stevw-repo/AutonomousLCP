# Hong Kong Regulatory Materials renderer status

The source-neutral ADR 0073 English tree, canonical renderer, table/form
closure, recursive official-boundary partitioner, and complete branch coverage
proof are frozen as conformance contract `1.0.0`. They issue no Search Record
and grant no serving authority.

The complete 62-case source-authority, membership, ownership, and required-
English/optional-Chinese checkpoint is also executable and source-neutral. It
proves only the frozen synthetic Legal Desk decisions, not real-source currency
or completeness.

The complete 51-case applicability-branch effective-state and transition
checkpoint is executable on the same strict evidence-to-decision boundary. It
retains atomic branches, exact component summaries, record responsibilities,
effective-fact ordering, and ADR 0005 outage consequences without authorizing
a Search Record, rebuild, or serving action.

The complete 30-case record-boundary, governing-dependency, definition,
source-structure, cross-reference, and hard-limit checkpoint is executable and
source-neutral. It preserves complete normal units and minimum dependency
closure, rejects recursive target copying and presentation-only identity, and
quarantines unresolved structure or targets without issuing a Search Record.

The complete 35-case canonical English rendering, table, fee, Regulatory Form,
English-only serving, measurement, and controlled authority-note checkpoint is
also executable and source-neutral. It compares exact canonical bytes and both
measurements, rejects incomplete projections and Chinese serving paths, and
records only identity consequences without allocating a Search Record.

The complete 24-case exact-limit and official-structure partition checkpoint is
also executable and source-neutral. It runs the accepted ADR 0073 recursive
partitioner, measures both exact ceilings over final labelled parts, proves
minimum-part earliest-full source-ordered groups, rejects every unsupported cut
or context removal, and emits exact branch quarantine and Coverage Gap
consequences without issuing a Search Record.

The complete 20-case English source-unit coverage and per-board readiness
checkpoint is also executable and source-neutral. It runs the accepted ADR
0073 coverage proof, requires exact primary/context/presentation and non-serving
accounting, detects missing, duplicate, reordered, orphaned, wrong-board,
unlabelled-dependency, and source-fidelity failures, keeps Main Board and GEM
bounded failures independent, and rejects count/parser/text proxies without
issuing a Search Record or authorizing promotion.

The complete 24-case Search Record identity, lineage, and traceability
checkpoint is also executable and source-neutral. It reuses the accepted
immutable record-identity and component-continuity decisions, keeps identical
wording board-owned, distinguishes exact reuse/reselection from every six-field
successor, validates one-to-one/split/merge forward lineage and complete lookup
entries, rejects cycles, collisions, ownership or fingerprint drift, and
quarantines similarity-only continuity. It allocates no ID and grants no
selection, embedding, release, or serving authority.

All 284 permanent cases and all 57 positive/near-miss pairs are now executable.
That closes the source-neutral case universe only; it is not authentic-source
evaluation, legal/system attestation, real-scope admission, or activation.

No authentic HKEX source-format parser, real component tree, exact admitted
embedding tokenizer/profile, table grammar derived from authentic artifacts, or
production renderer activation is admitted. Both board scopes remain
`NOT_READY`.
